INCLUDE_DIRECTORIES(include)
AUX_SOURCE_DIRECTORY(. DIR_PSEUDOPUREFLUIDS_SRCS)  

ADD_LIBRARY(pseudopurefluids_static STATIC ${DIR_PSEUDOPUREFLUIDS_SRCS})  
SET_TARGET_PROPERTIES(pseudopurefluids_static PROPERTIES OUTPUT_NAME "pseudopurefluids")  
SET_TARGET_PROPERTIES(pseudopurefluids_static PROPERTIES CLEAN_DIRECT_OUTPUT 1)  
  
ADD_LIBRARY(pseudopurefluids_shared SHARED ${DIR_PSEUDOPUREFLUIDS_SRCS})  
SET_TARGET_PROPERTIES(pseudopurefluids_shared PROPERTIES OUTPUT_NAME "pseudopurefluids")  
SET_TARGET_PROPERTIES(pseudopurefluids_shared PROPERTIES CLEAN_DIRECT_OUTPUT 1) 
