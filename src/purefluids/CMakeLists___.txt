INCLUDE_DIRECTORIES(include)
AUX_SOURCE_DIRECTORY(. DIR_PUREFLUIDS_SRCS)

ADD_LIBRARY(purefluids_static STATIC ${DIR_PUREFLUIDS_SRCS})  
SET_TARGET_PROPERTIES(purefluids_static PROPERTIES OUTPUT_NAME "purefluids")  
SET_TARGET_PROPERTIES(purefluids_static PROPERTIES CLEAN_DIRECT_OUTPUT 1)  
  
ADD_LIBRARY(purefluids_shared SHARED ${DIR_PUREFLUIDS_SRCS})  
SET_TARGET_PROPERTIES(purefluids_shared PROPERTIES OUTPUT_NAME "purefluids")  
SET_TARGET_PROPERTIES(purefluids_shared PROPERTIES CLEAN_DIRECT_OUTPUT 1) 
